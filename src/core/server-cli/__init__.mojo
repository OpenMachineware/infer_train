# core/server-cli/__init__.mojo
#
# M10 server entry points: it-server (llama-server-compatible HTTP
# service) and it-rpc-server (the multi-process / multi-machine RPC
# worker).  Shared CLI/generation code lives in core/cli_common, the
# HTTP layer in core/http.
